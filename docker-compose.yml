services:
  frontend:
    build:
      context: .
      dockerfile: ./docker/Dockerfile.production
    ports:
      - "8002:80"
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "3"




